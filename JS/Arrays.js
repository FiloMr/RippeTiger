/*let tab =[2,4,6,8];
alert(`drugi element tablicy to ${tab[1]}`);
alert(`dlugosc tablicy to ${tab.length}`);
tab.push(1,7);
alert(`dlugosc tablicy to ${tab.length}`);
tab[1]=9;
alert(`cała tablica to ${tab}`);
*/
/*const tytulAutor = [
    ["Gdziekolwiek", "Edwart Stachura"],
    ["Wyspa", "Jonasz Kofta"],
    ["Życie", "Jan"]
];

for (let i=0;i<tytulAutor.length; i+= 1)
{
    alert("Tytuł wiersza to "+ tytulAutor[i][0] +" Autorstwa "+ tytulAutor[i][1]);
}

for (let i=0;i<tytulAutor.length; i++)
{
    for (let k=0;k<2; k++)
    {
        alert(tytulAutor[i][k]);
    }
}
*/
/*let sum= 0;
let liczby=[1,2,3,54,6,78,9,10];
for (let liczba of liczby)
{
    sum += liczba;
}
alert("suma liczb w tabeli to "+ sum);
*/
let zdanie ="Witaj w świecie";
for (let znak of zdanie)
{
    alert(znak);
}